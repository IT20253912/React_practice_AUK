function Main(props) {
  const name = "name test";

  return (
    <div>
      {/* This is main components
      {props.children} */}

      <span>{name}</span>
    </div>
  );
}

export default Main;
